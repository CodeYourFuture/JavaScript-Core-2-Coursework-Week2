/*const londonLocations = {
  Angel: ["tube", "bus"],
  "London Bridge": ["tube", "river boat"],
  "Tower Bridge": ["tube", "bus"],
  Greenwich: ["bus", "river boat"],
};

function journeyPlanner(locations, transportMode) {
  const specificTransportModeLocation = [];
  for (let location in locations) {
    if (londonLocations[location].includes(transportMode)) {
      specificTransportModeLocation.push(location);
    }
  }
  return specificTransportModeLocation;
}
console.log(journeyPlanner(londonLocations, "bus"));*/

const lorena = {
  age: 34,
  name: ["lorena", "capraru"],
};

for (let el in lorena) {
  console.log(lorena[el]);
}
//alin
